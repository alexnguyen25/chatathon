# XcodeGen spec. The repository has no .xcodeproj checked in — it is generated
# from this file, which keeps the project structure reviewable as text and
# avoids merge conflicts in a pbxproj nobody can read.
#
#   brew install xcodegen && cd ios && xcodegen generate
name: LowCor

options:
  bundleIdPrefix: com.lowcor
  deploymentTarget:
    iOS: "17.0"
  createIntermediateGroups: true

packages:
  StressCore:
    path: StressCore
  StressMonitorUI:
    path: StressMonitorUI

targets:
  LowCor:
    type: application
    platform: iOS
    sources:
      - path: App/Sources
    configFiles:
      Debug: Config/Base.xcconfig
      Release: Config/Base.xcconfig
    settings:
      base:
        INFOPLIST_FILE: App/Info.plist
        GENERATE_INFOPLIST_FILE: NO
    dependencies:
      - package: StressCore
        product: StressCore
      - package: StressMonitorUI
        product: StressMonitorUI

  # Exists to produce screenshots in CI, not to assert behaviour. Nobody on
  # this project has a Mac, so this is the only way the screens get looked at.
  LowCorUITests:
    type: bundle.ui-testing
    platform: iOS
    sources:
      - path: UITests
    dependencies:
      - target: LowCor
    settings:
      base:
        TEST_TARGET_NAME: LowCor

schemes:
  LowCor:
    build:
      targets:
        LowCor: all
        LowCorUITests: [test]
    test:
      targets:
        - LowCorUITests
